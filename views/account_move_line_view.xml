<odoo>

    <record id="account_move_line_ipc_tree" model="ir.ui.view">
        <field name="name">account.move.line.ipc.tree</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree"/>
        <field name="arch" type="xml">
            <field name="amount_residual" position="after">
                <field name="debt_balance_ipc" optional="hide"/>
            </field>
        </field>
    </record>

</odoo>
